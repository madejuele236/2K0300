# LS2K0300 Vendor 与 `new/code` 联合重构计划

## Summary

将 `new/code/platform/true_ls2k0300` 建成 LS2K0300 驱动的唯一权威实现，逐模块替代 `true_LS2K0300_Library` 中参与主程序构建的 vendor 驱动。

已锁定的决策：

- 最终主源位于 `new/code`，不继续在 vendor 树中维护驱动实现。
- 只保障 `new/code`、`new/user` 主程序；vendor 示例和 `primer_code` 不属于兼容范围。
- 允许破坏现有 bridge API，不保留长期 facade。
- 采用逐模块原子切换：接口、调用者、构建归属和测试在同一模块阶段一起完成。
- 已验证硬件特性必须保留，包括特殊字符设备读语义、persistent-fd/fallback 模式和必要的硬件访问序列。
- UVC 必须实现真实的 start/capture/stop/restart 生命周期，不保留空 `ShutdownCamera()`。
- Timer、Encoder、IMU、Motor 热路径零动态分配、无异常。
- 每个模块必须通过 host、构建和板端 A/B 门禁；失败后持续修复至通过，不跳过。
- 迁移顺序按风险递增。
- 被替代的 vendor 驱动整体移入仓内 `archive/` 快照；TFLM、NCNN 等必要第三方保持原位置和上游形态。

## Target Architecture and APIs

### 1. 构建与目录所有权

建立以下 target，主程序只通过 target 依赖，不再全局暴露 vendor include 目录：

- `ls2k_linux_io`：fd、read/write/lseek/poll 等 Linux 机制。
- `ls2k_platform_timer`
- `ls2k_platform_adc`
- `ls2k_platform_encoder`
- `ls2k_platform_imu`
- `ls2k_platform_camera`
- `ls2k_platform_motor`
- `ls2k_assistant_protocol`
- `ls2k_assistant_transport`
- `vendor_tflm`
- `vendor_ncnn`

每个 target 使用显式源文件列表和 `PUBLIC`/`PRIVATE` 依赖。每个阶段输出实际 translation unit、object 和链接符号清单。

vendor 文件只有在同时满足以下条件后才能退出主构建：

1. 主程序不存在指向旧实现的调用路径。
2. 旧 translation unit 不再编译。
3. 最终二进制不存在旧实现符号。
4. 新实现通过 host 契约测试。
5. 新旧实现在板端原始或等价条件下通过 A/B 验证。
6. 实时模块满足最坏耗时和抖动门槛。

### 2. Linux I/O 基础层

在 `new/code/platform/linux` 建立不包含设备策略的机制层：

- `UniqueFd`
  - move-only；
  - 默认无效 fd；
  - 析构关闭；
  - `Reset()` 和 `Release()`；
  - 禁止复制。
- `IoResult`
  - 实际传输字节数；
  - 固定错误枚举；
  - 原始 errno；
  - 不包含动态字符串。
- `SyscallApi`
  - `open/read/write/lseek/close/poll` 函数表；
  - production 使用真实 syscall；
  - host 测试注入 fake；
  - 在初始化时绑定，热路径不修改。
- 热路径不重试、不 fallback、不拼接诊断字符串；设备 owner 根据正式硬件契约决定策略。

禁止把下列策略放入通用层：

- encoder 的零字节成功规则；
- persistent-fd 到 open/read/close 的 fallback；
- IMU 型号和轴映射；
- Motor duty clamp、方向切换和初始化探测；
- Camera frame 生命周期；
- Assistant reconnect/backoff。

### 3. 设备接口

现有全局 bridge 函数按模块一次性替换为具体、move-only 设备对象；不使用热路径虚函数和异常。

- `TimerDevice`
  - `Start(period, callback)`、`Stop()`、`Running()`；
  - 保留现有 timerfd/eventfd/poll 语义；
  - vendor PIT 不合并，只作为行为基线。

- `AdcDevice`
  - 初始化时绑定路径；
  - `ReadRaw()` 返回固定 `AdcSampleResult`；
  - 单位保持原始 ADC count，不在驱动层引入电压换算。

- `EncoderPair`
  - 初始化时绑定左右设备路径；
  - 明确记录 persistent 或 open/read/close 模式；
  - `ReadCounts()` 一次返回左右计数和各自状态；
  - 保留经板端确认的 `read()==0` 特殊成功契约；
  - fallback 只在已验证触发条件下发生，并产生固定诊断码。

- `ImuDevice`
  - 初始化完成型号识别、路径解析和 fd 模式选择；
  - `ReadRawSample()` 返回同一采样周期的原始多轴数据；
  - 型号、轴顺序、符号、单位和必需通道数由 IMU owner 管理；
  - 样本任一必需通道失败时整体无效，不用零值补偿。

- `CameraDevice`
  - `Start(config)`、`Capture()`、`Stop()`；
  - `Stop()` 必须释放 fd、mmap/buffer 和采集状态；
  - `Capture()` 返回非拥有 frame view，并明确有效期只到下一次 capture、stop 或对象销毁；
  - 同一对象支持重复 start/stop；
  - 不再公开空 shutdown。

- `MotorDevice`
  - 聚合左右 PWM 和方向 GPIO 资源；
  - `Initialize()`、`Apply(left,right)`、`Stop()`；
  - duty 范围、方向切换顺序和零 PWM 门槛由 Motor owner 管理；
  - 保留已验证的初始化硬件访问序列；
  - 初始化失败必须进入明确的 PWM=0 安全状态；
  - 不允许通用 `WritableFd` 自行探测或切换 GPIO。

- `AssistantTransport`
  - 保留现有非阻塞 TCP、ready/connecting/backoff、reliable/drop-if-busy 行为；
  - 移除 vendor `seekfree_assistant_interface_init()` callback ABI；
  - `ls2k_assistant_protocol` 只做 byte-level 编解码，不知道 socket、DNS、重连或服务状态；
  - `assistant_service` 和 `assistant_link` 保持现有责任，不重新拆层。

## Implementation Sequence

### Phase 0 — 基线与所有权清单

- 记录每个主程序 target 的源码、object、符号和 include 依赖。
- 建立 vendor TU 到调用者、设备节点和替代模块的映射。
- 保存当前板端程序版本、参数和设备路径。
- 采集 Timer 周期、Encoder/IMU 读取、Motor 初始化、UVC 生命周期、Assistant 重连和 ADC 原始值基线。
- 对特殊硬件行为形成明确契约测试，禁止实现者按普通 POSIX 语义自行修改。

退出条件：所有待迁模块均有 producer、consumer、资源 owner、失败语义和可重复板端基线。

### Phase 1 — Target 化构建

- 先建立目标边界，不改变运行机制和初始化顺序。
- 将第三方 TFLM/NCNN 与项目驱动分离。
- 为每个 vendor TU 标注当前依赖理由。
- 对比分拆前后的 translation unit、object、链接对象和符号清单，并核对初始化顺序不变。
- 在等价板端运行条件下执行 target 化前后的 runtime smoke A/B，覆盖 Timer、ADC、Assistant、Encoder、IMU、Camera 和 Motor 初始化/关闭路径。若 target 化与后续逐模块迁移以同一实现提交交付、没有可执行的 target-only 中间版本，则不得伪造中间 run；改用 Phase 0 baseline 到最终整机 smoke 的端点 A/B、逐模块板端 A/B，以及 initialization/translation-unit/object/link/symbol 审计共同完成累计等价证明。
- Phase 1 只改变 target 边界；本阶段不允许任何 vendor translation unit 退出主构建。

退出条件：主程序和现有 host tests 构建通过；初始化顺序、translation unit、object、链接对象、符号及等价板端 runtime smoke 均证明 target 化前后行为等价；运行时源码集合无非预期变化；没有 vendor translation unit 在 Phase 1 退出主构建。使用同一实现提交交付时，前述累计端点证明是本阶段的显式替代门禁，vendor TU 仍只能由后续模块自己的完整门禁退休。

### Phase 2 — Linux I/O 基础层

- 实现 `UniqueFd`、`IoResult` 和 `SyscallApi`。
- 完成 fd 生命周期、EINTR、短读写、errno 保留和 move 语义测试。
- 暂不接入 UVC、Motor 或控制热路径。

退出条件：基础层 host tests 通过，且不包含任何设备策略或隐式恢复。

### Phase 3 — Timer 收口

- 将现有 `timer_bridge` 适配为最终 `TimerDevice`。
- 更新 bootstrap/control 调用者。
- 验证启动、周期回调、超周期累计、停止和线程退出。
- 从主构建移除 vendor PIT。

退出条件：板端周期和最坏抖动不劣于 Phase 0 基线，旧 PIT object/symbol 消失。

### Phase 4 — ADC 迁移

- 先确认 vendor ADC 与当前 sysfs reader 的真实调用和单位。
- 切换调用者到 `AdcDevice`。
- 移除 vendor ADC translation unit。

退出条件：路径、原始值、失败码和采样频率与基线一致。

### Phase 5 — Assistant 收口

- 保留现有 service/link/TCP 状态机。
- 将 vendor assistant 中仍需要的协议逻辑迁为 `ls2k_assistant_protocol`。
- 删除 callback ABI，改为显式协议输入输出。
- 从主构建移除 vendor assistant glue 和无调用的 vendor TCP/UDP。

退出条件：host golden vectors、partial I/O、would-block、peer-close、backoff 和板端断线重连全部通过。

### Phase 6 — Encoder 迁移

- 实现 `EncoderPair` 并一次性更新控制调用者。
- 保留已验证的特殊 read 返回规则和两种 fd 模式。
- 不改变计数位宽、符号或左右轮映射。
- 移除 vendor encoder/file 依赖中已无其他 owner 使用的部分。

退出条件：静止、正转、反转、persistent 和 fallback 模式板端 A/B 一致，控制周期最坏耗时达标。

### Phase 7 — IMU 迁移

- 将路径解析、型号识别、channel 所有权和样本组装收进 `ImuDevice`。
- 迁移 vendor IMU core 中仍有效的型号知识，不复制全局状态结构。
- 一次性更新 IMU adapter 和控制消费者。
- 移除被替代的 vendor IMU translation units。

退出条件：所有实际支持型号的轴顺序、符号、单位、样本有效性和热路径时序通过板端 A/B。

### Phase 8 — UVC 迁移

- 明确 vendor UVC 的 fd、mmap、buffer 和阻塞模型。
- 实现真正的 `CameraDevice::Stop()`。
- 一次性更新 camera frame source。
- 移除全局 `rgay_image` 和编译期宽高作为跨层共享状态。

退出条件：多轮 start/capture/stop/restart 成功，fd 和内存不增长，帧尺寸、格式和有效期符合契约。

### Phase 9 — Motor 迁移

- 在安全架台上记录初始化 PWM/GPIO 写序列和车轮状态。
- 实现聚合式 `MotorDevice`，保留经验证的初始化访问序列。
- 一次性更新 actuator adapter 和控制调用者。
- 验证任何失败路径均输出 PWM=0，且无未经授权的方向脉冲或运动。
- 最后移除 vendor PWM/GPIO/file 驱动。

退出条件：初始化、正反转、零命令、方向切换、persistent/fallback 和故障注入全部通过板端验收，热路径最坏耗时不退化。

### Phase 10 — 归档与清理

- 将已被替代的 vendor 驱动按原目录结构整体移到仓内 `archive/`。
- 附带来源版本、文件哈希、原路径到新 owner 的迁移表和最后验证证据索引。
- `archive/` 不参与 include、编译或链接。
- TFLM、NCNN、许可证和仍参与构建的必要第三方保持原位置。
- 删除临时兼容接口、旧 include 路径和无调用类型。
- 更新架构文档，声明 `new/code` 为唯一主源。

实际移动或删除 vendor 源码前再次进行精确 inventory；不删除日志、参数或证据包。

### Phase 11 — 完成证据索引

实现代理必须在本文件末尾追加“完成证据索引”，但不得改写或弱化前述要求。索引至少包含：

- 最终 commit 和工作树状态；
- 每个阶段涉及的最终 target 和 owner；
- host 测试命令及结果；
- 交叉构建命令及结果；
- object/symbol 审计命令及结果；
- 每个模块的板端证据路径、运行标识和结果；
- 时序、资源、UVC 重启、Motor 安全和 Assistant 重连证据；
- 已归档 vendor 文件的 manifest 路径；
- 任何无法执行或未覆盖的检查；存在此类项目时不得声称任务完成。
- 每次独立验证的 invocation、findings、verifier evidence 和 caller-owned current `agent-table.json` 路径；
- reviewer 是否为全新实例、是否使用 `fork_turns="none"`、实际 `review_phase`、明确的 source-first 审阅范围、coverage 和最终 verdict 字段。

该索引的目的，是让不接收任何对话上下文的审阅代理仅通过本计划文件即可定位并复核全部实现和证据。

### Phase 12 — 无上下文独立验证

所有实现、集成、常规测试、板端验证、归档和证据索引完成且工作树稳定后，必须创建全新的 `verify-reviewer`：

- 必须使用 `fork_turns="none"`；
- 不得复用参与过设计、实现或先前验证的 agent；
- 验证运行期间不得修改被审阅范围；
- prompt 不得复述实现摘要、预期结论、已知风险、测试结果或主线程判断；
- prompt 必须是以下最小 standalone bundle；它只声明审阅契约和证据定位，不携带任何父线程对话上下文、实现摘要或结果：

```json
{
  "target_ref": "repo:2K0300-ls2k0300-vendor-refactor-2026-07-13",
  "review_goal": "implementation_correctness",
  "review_phase": "source_first",
  "risk_tier": "STRICT",
  "evidence_paths_or_diff_scope": [
    "/home/madejuele/projects/2K0300/plan-20260713.md",
    "git-diff:7aa9ca284ae681651de9d9f3c3781b15f5a73ff5..c9e2277e7a56f86dffacd6786c06c65b8cfad6f0",
    "/home/madejuele/projects/2K0300/new/verification/vendor-refactor-20260713/EVIDENCE.md",
    "/home/madejuele/projects/2K0300/archive/true_ls2k0300_vendor_drivers_20260713/metadata/MANIFEST.tsv"
  ],
  "findings_contract": "shared-findings-v1",
  "output_paths": {
    "findings_path": "/home/madejuele/projects/2K0300/new/verification/vendor-refactor-20260713/verification-cycle/attempt-2/findings.json",
    "verifier_evidence_path": "/home/madejuele/projects/2K0300/new/verification/vendor-refactor-20260713/verification-cycle/attempt-2/verifier-evidence.json",
    "agent_table_path": "/home/madejuele/projects/2K0300/new/verification/vendor-refactor-20260713/verification-cycle/agent-table.json"
  }
}
```

调用元数据固定为 `agent_type=verify-reviewer`、`fork_turns="none"`。caller 在 reviewer 返回后把原始 findings/evidence 持久化到 bundle 声明的 reviewer-owned 路径，并更新 caller-owned `agent-table.json`；这一结果持久化属于非实现元数据，不改变受审实现提交，也不单独使 verdict 失效。任何源码、测试或计划的 material change 仍会使 verdict 失效，必须再次执行全新的无上下文验证。

独立 reviewer 必须自行从计划文件发现：

- 原始目标和验收标准；
- 当前源码与构建归属；
- 可执行测试和构建入口；
- 板端证据位置；
- 归档和迁移 manifest；
- 未验证风险。

结果处理：

- `PASS`：只有此结果允许主线程声称联合重构完成；
- `FAIL`：主线程修复 findings、重新执行受影响测试和板端门禁、更新完成证据索引，然后创建另一个全新的 `verify-reviewer`；不得复用原 reviewer；
- `BLOCKED`：补齐缺失环境或证据后，创建另一个全新的无上下文 reviewer；若无法补齐，报告阻塞，不得声称完成；
- 任何 material change 都使旧 verdict 失效，必须再次执行全新的无上下文验证；
- agent 输出只作为证据；主线程仍负责核对高影响引用、最终集成判断和用户报告。

## Test and Acceptance Plan

- 每个模块必须按顺序通过：静态调用审计 → host tests → 交叉构建 → object/symbol 审计 → 板端 A/B → 时序/资源检查。
- 测试失败后保持在当前模块修复并复测，不进入下一阶段。
- Timer、Encoder、IMU、Motor 周期调用：
  - 零动态分配；
  - 不抛异常；
  - 不进行字符串拼接；
  - 记录最坏耗时和抖动，不只记录平均值。
- Motor 必须在安全架台上验证初始化和故障场景。
- Camera 必须验证重复启停、首帧、空帧、格式、fd 和内存稳定性。
- Assistant 必须覆盖 DNS 失败、连接中、短写、would-block、断开、backoff、恢复和两种发送策略。
- 任何 vendor TU 只有在调用、构建和板端行为三类证据都通过后才允许归档。
- 最终必须取得全新、无上下文 `verify-reviewer` 的 `PASS`。

## Assumptions

- `new` 主程序是唯一必须保持工作的应用；旧示例和 `primer_code` 仅用于证据对照。
- 当前已经验证的字符设备特殊行为是正式硬件契约，除非新板端证据推翻，不在重构中“纠正”。
- C++17 保持不变。
- 初始化和非实时 Assistant/UVC 路径可以使用受控动态分配；控制周期不能。
- 不在本计划中重构 TFLM、NCNN、Linux 4.19 内核或其他真正第三方内部源码。
- 不创建长期双 API；每个模块完成原子切换后，旧 bridge API 在同阶段删除。

## 完成证据索引

### 1. 受审版本与工作树边界

- 仓库根目录：`/home/madejuele/projects/2K0300`。
- 最终实现提交：`c9e2277e7a56f86dffacd6786c06c65b8cfad6f0`（`refactor(ls2k0300): own platform drivers in new code`）。
- 本索引和 `new/verification/vendor-refactor-20260713/verification-cycle/` 位于上述实现提交之后的 metadata-only 工作范围。独立 reviewer 应直接审阅固定实现 diff `7aa9ca284ae681651de9d9f3c3781b15f5a73ff5..c9e2277e7a56f86dffacd6786c06c65b8cfad6f0`，并执行：

  ```text
  rtk git rev-parse c9e2277e7a56f86dffacd6786c06c65b8cfad6f0
  rtk git diff --name-only 7aa9ca284ae681651de9d9f3c3781b15f5a73ff5..c9e2277e7a56f86dffacd6786c06c65b8cfad6f0
  ```

  第一条必须解析为同一实现提交；第二条给出本次 implementation source-first 的完整固定变更面。调用时另行检查工作树，除本计划、verification-cycle 元数据和明确列出的用户文件外若存在实现源码或测试变化，不得沿用本索引。
- 实现提交后的 `rtk git status --short --untracked-files=all` 只有以下两个未跟踪、未暂存、且不属于本次重构的用户文件：

  ```text
  ?? new/verification/tests/bev_connectivity_aligned_replay
  ?? primer_code/primer_code.zip
  ```

  受审的 tracked 文件无未提交修改。上述两个文件未被读取为验收证据、未被删除、未被归档、未进入提交。
- 最终 LoongArch 主程序本地与板端 SHA-256 均为 `d97f0dbc81095ae1a68de2b1d2854b4fb316f0538eeb732e35eb0c3c4f75c13d`；板端路径为 `/home/root/new`。
- 总证据目录：`/home/madejuele/projects/2K0300/new/verification/vendor-refactor-20260713`；详细逐项索引为该目录下的 `EVIDENCE.md`。

### 2. 最终 targets 与 owner

target 定义和显式源文件表位于 `new/user/CMakeLists.txt`；稳定所有权说明位于 `new/docs/ls2k0300-platform-ownership.md`。

| Phase | 最终 target | 唯一 owner / 责任边界 |
| --- | --- | --- |
| 1-2 | `ls2k_linux_io` | `new/code/platform/linux/linux_io.*`：`UniqueFd`、`IoResult`、`SyscallApi`，不含设备策略 |
| 3 | `ls2k_platform_timer` | `TimerDevice`；timerfd/eventfd/poll 生命周期与回调线程 |
| 4 | `ls2k_platform_adc` | `AdcDevice` 与 power adapter；原始 ADC count |
| 5 | `ls2k_assistant_protocol`、`ls2k_assistant_transport` | byte-level 协议与既有非阻塞 TCP/service/link 状态机 |
| 6 | `ls2k_platform_encoder` | `EncoderPair`、`encoder_mapping.hpp` 与 encoder adapter；raw 读取和逻辑符号分层 |
| 7 | `ls2k_platform_imu` | `ImuDevice` 与 IMU adapter；型号、channel、完整样本有效性 |
| 8 | `ls2k_platform_camera` | `CameraDevice`、camera adapter/frame source；fd、mmap、buffer、frame view 生命周期 |
| 9 | `ls2k_platform_motor` | `MotorDevice` 与 actuator adapter；左右交叉通道、方向、PWM、ESC、safe stop |
| 10 | `vendor_tflm`、`vendor_ncnn` | 原样保留的必要第三方；不属于项目驱动重构 |

旧 `new/code/platform/true_ls2k0300/bridge.hpp` 已删除。主程序不再通过全局 bridge API 或 vendor include 路径取得这些设备。

### 3. Host 契约测试

2026-07-13 在仓库根目录重跑以下全部命令，均退出 `0`：

```text
rtk new/verification/tests/run_linux_io_test.sh
rtk new/verification/tests/run_timer_device_test.sh
rtk new/verification/tests/run_adc_device_test.sh
rtk new/verification/tests/run_assistant_protocol_contract_test.sh
rtk new/verification/tests/run_assistant_bridge_contract_test.sh
rtk new/verification/tests/run_encoder_pair_test.sh
rtk new/verification/tests/run_imu_device_test.sh
rtk new/verification/tests/run_camera_device_test.sh
rtk new/verification/tests/run_motor_device_test.sh
rtk new/verification/tests/run_power_adapter_threshold_test.sh
rtk new/verification/tests/run_camera_frame_source_test.sh
rtk new/verification/tests/run_camera_frame_store_test.sh
rtk bash new/verification/tests/run_assistant_telemetry_selftest.sh
rtk new/verification/tests/run_startup_low_voltage_order_test.sh
rtk new/verification/tests/run_hot_path_allocation_contract_test.sh
```

可见结果字符串和无输出但 exit `0` 的项目逐条记录在 `new/verification/vendor-refactor-20260713/EVIDENCE.md` 的 “Host contract tests” 表中。热路径测试同时包含 `noexcept` 静态断言，并观测 `EncoderPair::ReadCounts()`、`ImuDevice::ReadRawSample()`、`MotorDevice::Apply()` 的全局动态分配数为 `0`。

### 4. 交叉构建与静态/object/symbol 审计

最终配置与构建命令：

```text
rtk cmake -S new/user -B new/out-final-evidence -DLS2K_BUILD_VERIFICATION_HELPERS=ON -DLS2K_TFLM_OP_BENCH=OFF
rtk cmake --build new/out-final-evidence --target new camera_device_board_test timer_device_board_test motor_device_board_test assistant_bridge_board_test encoder_pair_board_test -j2
```

两条命令均退出 `0`。`rtk file` 将六个输出全部识别为 64-bit LoongArch Linux ELF。

旧调用/include 审计：

```text
rtk rg -n '#include[[:space:]]+[<"](zf_|.*zf_)|seekfree_assistant_interface_init|uvc_camera_init|pit_ms_init|encoder_init|pwm_init|gpio_init' new/code new/user --glob '!out/**' --glob '!out-*/**'
```

结果为无匹配（`rg` exit `1`）。最终 vendor object 审计：

```text
rtk /usr/bin/find new/out-final-evidence -type f -name '*.o' | rtk /usr/bin/grep -E '/(zf_driver|zf_device|zf_common|seekfree_assistant)/' | rtk /usr/bin/wc -l
```

结果为 `0`；同一表达式在 baseline build 中为 `19`。最终旧符号审计：

```text
rtk /opt/ls_2k0300_env/loongson-gnu-toolchain-8.3-x86_64-loongarch64-linux-gnu-rc1.6/bin/loongarch64-linux-gnu-nm -C new/out-final-evidence/new | rtk rg -i '(^|[^a-z])pit|uvc_camera|seekfree_assistant'
```

结果为无匹配（`rg` exit `1`）；baseline 对应表达式为 7 行。活动源码、测试、文档的 staged whitespace 检查通过；archive 中的上游源码保持原字节，不作格式化重写。

Phase 1 的 target 化门禁要求核对初始化顺序、translation unit、object、链接对象和符号，且该阶段不退休 vendor TU。本次 target 化与逐模块迁移在同一实现提交交付，没有可执行的 target-only 中间版本，因此不虚构该 run；按 Phase 1 的显式累计交付门禁，使用 `7aa9ca2` baseline 与最终 object/symbol/runtime smoke 的端点 A/B，再结合每个模块各自的 host、构建和板端门禁完成证明。该证据证明最终累计结果，不声称能把每个运行时差异单独归因到 target 化步骤。

### 5. 板端模块证据

板端为 `root@192.168.137.100`。通过 Windows OpenSSH 上传；每个执行产物均在本地和板端比较 SHA-256 后运行。

| 模块 / gate | 证据路径 / run 标识 | 结果 |
| --- | --- | --- |
| Phase 0 baseline | `runtime-smoke-baseline-head-7aa9ca2.log`；commit `7aa9ca284ae681651de9d9f3c3781b15f5a73ff5` | ADC、IMU、Encoder、Camera、Assistant、Timer 均进入运行路径并退出 `0` |
| Timer | `timer-ab-20260713.log` | baseline 最坏绝对抖动 `111 us`；最终 1000 样本最坏 `105 us`，无 missed expiration/overrun |
| ADC / Power | 最终 runtime logs 与 host ADC/power tests | `/sys/bus/iio/devices/iio:device0/in_voltage7_raw` 绑定成功；启动低压检查 clear；Motor 窗口 250 次 ADC 采样均为 0 错误 |
| Assistant | `assistant-board-coldboot-20260713.log`；协议/bridge host tests；最终 runtime log | 独立板端 contract 成功连接并 exit `0`；host 覆盖 DNS/connecting/partial I/O/would-block/peer-close/backoff/recovery/reliable/drop；最终 smoke 在主机端无 listener 的真实条件下明确观测 8 次 refusal/backoff，不把该 smoke 误写成连接成功 |
| Encoder | `encoder-board-coldboot-20260713.log`；`runtime-smoke-post-motor-perf-20260713.log` | 特殊零字节/32-bit 隔离契约与 20 次重复进程通过；最终五个窗口中 read 最坏 `147 us`；正反转符号由 Motor A/B 再验证 |
| IMU | baseline、cold-boot 与最终 runtime logs | 实际型号 IMU660RA；32 连续有效样本；最终五个窗口中完整 sample read 最坏 `95 us` |
| Camera/UVC | `camera-restart-coldboot-20260713.log` | 同一对象 3 轮 start/capture/stop，每轮 5 帧；最终 fd `6`、stop 后虚拟页 `322`，无增长，exit `0` |
| Motor 物理极性 A/B | `motor-clean-3000-5s-20260713.log`、`motor-clean-forward-3000-5s-20260713.log` 及配对 PWM logs | 被拒绝实现 `hardware_duty=-logical` 时用户确认双轮后退；仅改为 `hardware_duty=logical` 后用户确认正常前进。最终 raw `+739/-877` 归一化为 `+739/+877` |
| Motor 正反转/换向 | `motor-clean-direction-switch-20260713.log`、pre/pwm logs | 前进 `+710/+826`，PWM=0 保持 150 ms 后反向 `-758/-870`；Apply `52/74 us`；ADC 0 错误；Stop PASS；最终 duty `0` |
| Motor fd 模式/最坏耗时 | persistent/fallback 两组 `motor-clean-*-3000-5s-20260713.log` 及 PWM logs | 各 250 次 Apply；persistent min/max/mean `31/96/31 us`，open-write-close `49/117/50 us`；两组均正常前进、ESC=0、最终 PWM=0 |
| Motor 故障安全 | `motor-clean-failure-safe-stop-pre-20260713.log`、`motor-clean-failure-safe-stop-20260713.log` | 第二路 drive write 注入一次 `EIO(5)`；production `FailSafeAfter` 后 owner 失效，显式 Stop 成功，helper 报告 `final_pwm=0`；注入前四路为 0 |
| 最终整机 smoke | `runtime-smoke-post-motor-20260713.log` | 最终二进制；actuator-disabled；449 frame lines；IMU continuity 32；Camera/Timer/Encoder 完整启动关闭；受控回到 DISARMED；runtime/copy exit 均 `0` |
| 最终实时窗口 | `runtime-smoke-post-motor-perf-20260713.log` | 293 frame lines、5 个 1 s perf 窗口；control tick/IMU/Encoder 最坏 `1416/95/147 us`；受控退出 `0` |
| 最终资源/安全态 | `runtime-smoke-post-motor-post-state-20260713.log` | 无残留 runtime 进程；板端主程序哈希匹配；两路 Motor 和两路 ESC duty 均为 `0` |

Motor 诊断过程、旧/新序列差异以及为何板端物理观察推翻历史 `-logical_duty`，详见 `new/verification/vendor-refactor-20260713/motor-old-new-sequence-comparison-20260713.md`。失败调查日志被保留为诊断证据，没有伪装成 PASS。

### 6. 归档和第三方边界

- 归档报告：`archive/true_ls2k0300_vendor_drivers_20260713/ARCHIVE_REPORT.md`。
- manifest：`archive/true_ls2k0300_vendor_drivers_20260713/metadata/MANIFEST.tsv`，1 行 header + 43 个文件。
- 完整性命令（在 archive 根目录执行）：

  ```text
  rtk awk -F '\t' 'NR > 1 { print $1 "  " $3 }' metadata/MANIFEST.tsv | rtk sha256sum -c -
  ```

  43 个文件全部返回 `OK`。
- live vendor `libraries/` 只保留 README 与 `zf_components/tflm`、`zf_components/ncnn`；archive 不出现在任何最终 include/source/link 输入。
- 未删除任何日志、参数或证据包。

### 7. 未覆盖项与 Phase 12 入口

- 所有实现、host、交叉构建、最终 object/symbol、板端、时序、资源、归档和最终整机 smoke 门禁均已执行。Phase 1 使用其“同一实现提交累计交付”替代门禁；证据映射是 `runtime-smoke-baseline-head-7aa9ca2.log`、`runtime-smoke-post-motor-20260713.log`、`runtime-smoke-post-motor-perf-20260713.log`、第 4 节 baseline/final object/symbol 结果和逐模块板端证据。不存在被隐瞒的 target-only run，也不把累计端点证据误称为中间提交隔离实验。
- attempt-1 是 contract-invalid 的 path-only 调用；它完成了 docs-first 计划审阅并返回 `block`，没有审阅 implementation source。原始调用、findings 和 execution evidence 分别位于 `verification-cycle/attempt-1/invocation.json`、`verification-cycle/attempt-1/findings.json` 和 `verification-cycle/attempt-1/verifier-evidence.json`。
- attempt-2 的精确 standalone source-first bundle 和 spawn metadata 位于 `verification-cycle/attempt-2/invocation.json`；caller-owned 当前状态位于 `verification-cycle/agent-table.json`。该 active row 记录 `fork_turns="none"`、显式 source-first scope、attempt-2 输出路径、`coverage_status=unknown` 和 `last_verdict=unknown`。
- attempt-2 reviewer 返回后，最终 findings 和 verifier evidence 必须分别持久化到 `verification-cycle/attempt-2/findings.json` 与 `verification-cycle/attempt-2/verifier-evidence.json`；目前不得提前创建。最终 evidence 必须记录 reviewer 的 agent id、fresh/fork-none 调用事实、实际 source-first reviewed paths/scope、coverage、时间和 final state，findings 必须提供 verdict 字段。
- reviewer 运行期间不得修改受审范围。caller 在返回后仅向 declared `output_paths` 持久化 reviewer 结果并更新 `agent-table.json`，属于非实现元数据且不改变受审实现提交；任何其他源码、测试或计划 material change 都使 verdict 失效。若 verdict 为 `FAIL` 或 `BLOCKED`，按 Phase 12 规则修复、重测、更新索引并重新验证；attempt-2 尚未运行，因此当前没有最终 verdict，在 `PASS` 前不得声称联合重构完成。
