# 基于原图 Boundary 的 BEV 路径 Connectivity 改造

## Summary

- 新增独立的 BEV 点对 Connectivity 能力：只投影两个 BEV 端点，沿投影后的原图线段零拷贝密集采样 YUYV 亮度；相邻样本继续使用 `abs(delta_y) >= LOCAL_JUMP_MIN_Y` 判定 Boundary。
- 普通路径不再依赖 `RowHasBoundaryJumpBetween()`、横向距离排名或“首次缺失立即结束”。每行按现有候选顺序检查，接受第一个 connectivity 通过的候选；当前行失败时继续检查更远行。
- 首个正式点从 BEV `{0,0}` 建立连接。当前不可见近端前缀由可信 origin-anchor 契约覆盖，可见子段必须无 Boundary。

## Interfaces and architecture

- 增加不依赖相机机制的 Connectivity 契约：
  - `BEVSegmentConnectivityStatus { kConnected, kBlocked, kUnobservable }`
  - `BEVSegmentVisibilityPolicy { kRequireFullSegment, kAllowFromEndpointClip }`
  - `BEVSegmentConnectivityQuery::Evaluate(from, to, policy)`
- 增加原图实现模块 `bev_image_segment_connectivity`：
  - 依赖 `CameraPixelFrameView`、`BEVProjector`、`BEVBoundaryParameters` 和 `SampleLumaAt()`。
  - 不知道候选、reference、路径行号和控制策略。
- 路径构造器只依赖 `BEVSegmentConnectivityQuery` 抽象，不知道 YUYV、投影和像素遍历。
- `RunBEVSimplePerception()` 负责组装具体原图 Connectivity evaluator，并调用：
  - `BuildReferencePath(rows, params, connectivity_query)`
- 将新 `.cpp` 加入 `new/user/CMakeLists.txt`；不复用 sparse-row LUT，因为 Connectivity 只投影端点。

## Connectivity and path behavior

- 端点投影成功后使用浮点 major-axis DDA：
  - `N = max(1, ceil(max(abs(delta_row), abs(delta_col))))`
  - 对 `i=0..N` 计算浮点图像坐标并调用 `SampleLumaAt()`。
  - 任意相邻样本满足原 `LOCAL_JUMP_MIN_Y` 判定即返回 `kBlocked`；全部成功且无 Boundary 返回 `kConnected`；投影、裁剪或采样无法满足策略时返回 `kUnobservable`。
- 普通点对必须使用 `kRequireFullSegment`，整段不可观察即不连接。
- 首点使用 `{0,0}` 和 `kAllowFromEndpointClip`：
  - 只允许 `from={0,0}` 一端位于当前标定的近端画面外；
  - 候选端必须在画面内；
  - 对进入画面后的完整可见子段执行 Boundary 检测；
  - 不可见前缀由“车辆原点位于当前道路内”的 anchor 契约覆盖。
- 路径构造改为：
  1. 保持现有候选生产及顺序：全部 midpoint → low-edge → high-edge → standalone jump。
  2. 初始 predecessor 为 `{0,0}`。
  3. 每行按顺序查询候选，接受第一个 `kConnected` 候选并更新 predecessor。
  4. 候选为空、全部 blocked 或 unobservable 时不输出、不更新 predecessor，也不终止；继续下一行。
  5. 输出继续压紧存储，并保留每个点真实 `forward_m`。
- 删除：
  - `RowHasBoundaryJumpBetween()`
  - `ChooseCenterCandidate()` 的 `best/best_cost` 和横向距离排名
  - `previous_lateral`
  - `segment_started`
  - 首 gap `break`
  - `ExtractStrictLeadingReferenceSegment` 及“第一个严格连续段”语义；改名为 connected-path 语义。
- 保留候选生成、boundary trace 支持/裁剪、reference usability、tracking geometry、时间 hold、特殊元素仲裁和 safety gate。

## Verification

- Connectivity 单元场景：
  - 水平、垂直和斜向线段无 Boundary → connected。
  - 线段穿过亮度跳变，含恰好达到阈值 → blocked。
  - 严格模式端点/线段越界或投影失败 → unobservable。
  - `{0,0}` 近端裁剪后可见段无 Boundary/有 Boundary分别接受和拒绝。
- 路径构造场景使用可控的 Connectivity fake：
  - 首点必须通过 origin connectivity。
  - 前几行无候选不影响后续首点。
  - 中间缺行、blocked 或 unobservable 不终止搜索。
  - 更远候选与最后接受点 connected 时成功重连。
  - 同一行多个 connected 候选时选择现有顺序的第一个。
  - 输出无数组 hole，跨 gap 点保留真实 `forward_m`。
- 更新原 `BuildReferencePath` 附属测试和旧的 jump/首-gap 预期；候选生成与 boundary trace 测试继续验证原责任。
- 运行 BEV perception、luma sampler、reference usability/tracking geometry 聚焦测试，完成全量 LoongArch 构建；随后使用当前板端原始帧或对齐采集做一次路径回放，确认跨 gap 重连且实际 Boundary 不被穿越。

## Assumptions

- 当前道路拓扑下，完整可观察且无 Boundary 的两点线段足以证明两点属于同一道路。
- BEV `{0,0}` 是车辆当前道路内的可信 anchor；当前约 0～0.039 m 的相机近端盲区不用于发现道路分叉。
- Connectivity 通过资格优先于一切几何接近度；不实现候选排序，多个通过者直接选现有容器中的第一个。
- 第一版不增加新阈值、窗口、分类器、滤波或 Connectivity 专用参数，Boundary 定义完全复用 `LOCAL_JUMP_MIN_Y`。
